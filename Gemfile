# frozen_string_literal: true

source 'https://rubygems.org'

# Specify your gem's dependencies in alt.gemspec
gemspec

gem 'irb' # to try the library out in bin/console
gem 'minitest' # to write the tests rake runs
gem 'rake' # to run the default task, which is the test suite
gem 'webmock' # to answer a lookup's two requests without reaching RapidAPI
